---
title: "PCA y FA de Phishing"
output: html_document
---



```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  message = FALSE,
	warning = FALSE,
	error = F,
  results='hide'
)

#Cargamos nuestras librerías 
# Cargamos nuestras librerías
library(tidyverse)     # ya incluye dplyr, ggplot2, readr, etc.
library(factoextra)
library(esquisse)
library(scatterplot3d)
library(patchwork)
library(here)
library(skimr)
library(corrplot)
library(vcd)
library(Rtsne)
library(ggsci)    
library(psych)     
library(grid) 
library(embed)
library(scattermore)
library(gganimate)
library(av)
library(RANN)  
library(Rtsne)
library(cluster)    
library(knitr)
library(kableExtra)


```

```{r Data}

data <- read.csv(here("Data", "Obesity_clean_2.csv"))






```

## Introducción a los Datos

El conjunto de datos **Obesity.csv** contiene información destinada a estimar los niveles de obesidad en función de los hábitos alimentarios, los antecedentes familiares y la condición física de los individuos. La base de datos incluye registros de personas de México, Perú y Colombia, abarcando un total de **2,111 observaciones** y **16 variables** relacionadas con el estilo de vida y la salud.

Las variables recogen información como el género, edad, altura, peso, antecedentes familiares de sobrepeso, frecuencia de consumo de ciertos alimentos, hábitos de hidratación, control de la ingesta calórica, actividad física, tiempo de uso de tecnología, consumo de alcohol y medio de transporte habitual. Además, se incluyen etiquetas que clasifican el nivel de obesidad, desde peso insuficiente hasta obesidad tipo III.

La mayoría de los datos fueron generados mediante técnicas sintéticas, mientras que algunos provienen directamente de usuarios a través de una plataforma web, lo que permite contar con un conjunto diverso y representativo para el análisis.



```{r}
#skim(data)

#analisis <- data 


#skim(analisis)
```
Para este reporte, se limpió la base de datos y a las siguientes variables se les asignaron los niveles descritos:

- **FCVC**: Indica la frecuencia con la que el individuo consume verduras.  
  Escalas: 1 = **Nunca**, 2 = **A veces**, 3 = **Siempre**.  

- **NCP**: Representa el número de comidas principales consumidas por día.  
  Escalas: 1 = **1–2 comidas**, 2 = **3 comidas**, 3 = **4 comidas**, 4 = **Más de 4 comidas**.  

- **CH2O**: Describe la cantidad de agua consumida diariamente.  
  Escalas: 1 = **Menos de 1L**, 2 = **1–2L**, 3 = **Más de 2L**.  

- **FAF**: Señala la frecuencia con la que la persona realiza actividad física.  
  Escalas: 0 = **Nunca**, 1 = **1–2 veces por semana**, 2 = **2–4 veces por semana**, 3 = **4–5 veces por semana**.  

- **TUE**: Cuantifica el número de horas al día frente a un dispositivo electrónico.  
  Escalas: 0 = **0–2 horas**, 1 = **3–5 horas**, 2 = **Más de 5 horas**.  


### Estadísticas descriptivas

En este estudio, los participantes presentaron en promedio:  

- **Edad**: 24 años  
- **Estatura**: 1.70 metros  
- **Peso**: 86.5 kilogramos  
- **Comidas principales**: Entre 3 y 4 por día  
- **Consumo de verduras**: Ocasional  
- **Consumo de agua**: 1–2 litros diarios  
- **Actividad física**: 1–2 veces por semana  
- **Uso de dispositivos electrónicos**: Entre 2 y 5 horas diarias  

A primera vista, estos resultados sugieren que los participantes no mantienen los **mejores hábitos de salud**, dado el consumo moderado de verduras y agua, la baja frecuencia de actividad física y la exposición considerable al uso de dispositivos electrónicos.


```{r, results='hide'}
data <- data %>%
  mutate(Gender = as.factor(Gender),
         CALC = as.factor(CALC),
         FAVC = as.factor(FAVC),
         SCC = as.factor(SCC),
         SMOKE = as.factor(SMOKE),
         family_history_with_overweight = as.factor(family_history_with_overweight),
         CAEC = as.factor(CAEC),
         MTRANS = as.factor(MTRANS),
         NObeyesdad = as.factor(NObeyesdad),
         FCVC <- as.factor(FCVC),
         NCP  <- as.factor(NCP),
         CH2O <- as.factor(CH2O),
         FAF  <- as.factor(FAF),
         TUE  <- as.factor(TUE)
        
         
  )
```


### Dividiendo los datos en continuos y en categoricos

```{r, results='hide'}
# Variables categóricas (factores o cadenas)
var_numeric <- data %>% dplyr::select(where(is.numeric), NObeyesdad) 


vars_cat <- data %>%  dplyr::select(where(is.factor))


#summary(vars_cat)

```


```{r,   fig.width=15, fig.height=17}
entradas <- c("Gender" ,"CALC", "FAVC", "SCC"  ,"SMOKE" ,"family_history_with_overweight" ,"CAEC" ,"MTRANS", "NObeyesdad", "FCVC", "NCP", "CH2O", "FAF", "TUE" )
graficasCat <- list()

for (i in entradas) {
  print(i)
  p <- ggplot(data, aes_string(x = i, fill = "NObeyesdad")) +
    geom_bar(position = "fill") +
    geom_text(
      stat = "count",
      aes(
        label = scales::percent(after_stat(count / sum(count)), accuracy = 0.1),
        group = NObeyesdad
      ),
      position = position_fill(vjust = 0.5),
      size = 3
    ) +
    ggsci::scale_fill_npg(name = "Nivel de obesidad", drop = FALSE) +
    labs(title = paste(i, "vs NObeyesdad")) +
    theme_minimal() +
    theme(
      axis.text.x   = element_text(angle = 45, hjust = 1),
      legend.text   = element_text(size = 7),
      legend.title  = element_text(size = 10),
      legend.key.size = unit(0.4, "cm")
    )

  graficasCat[[length(graficasCat) + 1]] <- p
}

wrap_plots(graficasCat, ncol = 3) +
  plot_layout(guides = "collect") &
  theme(
    legend.position = "right",       # ahora a la derecha
    legend.direction = "vertical",   # lista vertical
    legend.justification = "center",
    legend.title = element_text(size = 9),
    legend.text  = element_text(size = 8)
  )


```






En la gráfica anterior se observa cómo se distribuyen los diferentes **tipos de obesidad** dentro de nuestras variables categóricas.  
En cuanto al **género**, tanto hombres como mujeres presentan obesidad en distintas categorías, aunque en los hombres parece haber una proporción ligeramente mayor en obesidad tipo I y II, lo que podría sugerir una predisposición distinta según el sexo.  

Respecto al **consumo de comida con alto contenido calórico (FAVC)**, la asociación es clara: quienes consumen estos alimentos con mayor frecuencia muestran una proporción más alta de obesidad, especialmente en los tipos I y II.  
De manera similar, los participantes con **antecedentes familiares de sobrepeso** presentan una tendencia marcada hacia niveles más altos de obesidad, lo que respalda el papel del **factor genético/hereditario** en el desarrollo de esta condición.  

Finalmente, la **actividad física (FAF)** muestra un patrón consistente: quienes realizan ejercicio con mayor frecuencia tienden a tener menor obesidad, mientras que los participantes más **sedentarios concentran los valores más altos de obesidad tipo II y III**.

```{r, Graficas_Bonitas2,  fig.width=15, fig.height=8}
df_long_num <- data %>%
  dplyr::select(all_of(c("Age","Height","Weight","NObeyesdad"))) %>%
  tidyr::pivot_longer(cols = c("Age","Height","Weight"),
                      names_to = "Variable", values_to = "Valor")

boxplots <- ggplot(df_long_num, aes(x = NObeyesdad, y = Valor, fill = NObeyesdad)) +
  geom_boxplot(notch = TRUE, outlier.shape = 21, outlier.size = 1.5,
               outlier.alpha = 0.35, width = 0.7) +
  geom_jitter(aes(color = NObeyesdad), width = 0.15, alpha = 0.12,
              size = 0.7, show.legend = FALSE) +
  facet_wrap(~ Variable, scales = "free_y", ncol = 3) +
  ggsci::scale_fill_npg(drop = FALSE) +
  ggsci::scale_color_npg(drop = FALSE) +
  labs(title = "Boxplots por variable numérica vs NObeyesdad",
       x = NULL, y = NULL) +
  theme_minimal() +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)   # ← rota etiquetas X
  )

#Graficas de barras
pal_scale <- ggsci::scale_fill_npg(name = "NObesity", drop = FALSE)

class_levels <- c("Insufficient_Weight","Normal_Weight",
                  "Overweight_Level_I","Overweight_Level_II",
                  "Obesity_Type_I","Obesity_Type_II","Obesity_Type_III")
var_numeric$NObeyesdad <- factor(var_numeric$NObeyesdad, levels = class_levels)

vars <- c("Age", "Height", "Weight")
graficasNum <- list()

for (i in vars) {
  # Opción A: histograma apilado (tu comportamiento actual)
  p <- ggplot(var_numeric, aes_string(x = i, fill = "NObeyesdad")) +
    geom_histogram(bins = 30L, color = "white", linewidth = 0.2) +
    pal_scale +
    labs(title = paste0(i, " vs NObesity"), x = i, y = "Conteo") +
    theme_minimal(base_size = 12) +
     theme(
    panel.grid.minor = element_blank(),
    axis.text.x = element_text(angle = 35, hjust = 1)
  )
graficasNum[[length(graficasNum) + 1]] <- p
}

# Patchwork
(graficasNum[[1]] + graficasNum[[2]] + graficasNum[[3]] + patchwork::plot_layout(ncol = 3, guides = "collect"))/ boxplots 

```


En la siguiente gráfica se muestra la distribución y los box plots de las **variables continuas** .  
Podemos observar que la mayoría de los participantes en el estudio son **jóvenes entre 20 y 25 años**.  
Asimismo, la estatura más frecuente se concentra en el rango de **1.60 a 1.80 metros**.  

En cuanto al **peso**, se distinguen claramente tres patrones:  
- Los individuos con **peso insuficiente** se agrupan en valores menores a 50 kg.  
- El **peso normal** se concentra entre 50 y 80 kg.  
- A partir de los **80 kg** se observa una mayor proporción de participantes con **sobrepeso y distintos tipos de obesidad**, lo que confirma el peso corporal como el principal factor de diferenciación entre categorías de obesidad.

Analizando los **box plots**, se observa que la **edad** no es un factor diferenciador, ya que los tres tipos de obesidad se concentran en el mismo rango de **20 a 30 años**.  
De manera similar, la **altura** se distribuye principalmente en el intervalo de **1.60 a 1.80 metros**, lo que indica que esta variable tampoco discrimina entre los diferentes niveles de obesidad.


### Análisis de correlación

Para este análisis se aplicaron dos enfoques complementarios:

- **Variables continuas**: se calcularon coeficientes de correlación de Pearson, lo que permite medir la relación lineal entre dos variables cuantitativas.  
- **Variables categóricas**: se utilizó el **Coeficiente V de Cramer**, basado en el estadístico Chi-cuadrado. Este coeficiente es apropiado para medir la **fuerza de asociación** entre variables cualitativas y toma valores entre 0 (sin asociación) y 1 (asociación perfecta).

El uso de ambos métodos cuenta con sustento teórico: la correlación de Pearson es un estándar ampliamente aceptado para datos continuos, mientras que el V de Cramer es una extensión del Chi-cuadrado que evita problemas de dependencia con el tamaño de la muestra, lo que lo hace adecuado para el análisis de variables categóricas.

De esta forma, se obtiene una visión más completa de las posibles relaciones entre las variables de estudio, considerando tanto las continuas como las categóricas.

```{r Correlacion_Continueas,  fig.width=4, fig.height=4,  results='hide'}

var_numeric <- var_numeric %>% dplyr::select(-NObeyesdad )

#cor(var_numeric)

corrplot(cor(var_numeric), method = "circle", type = "upper", tl.col = "black", tl.srt = 45, addCoef.col = "black")
```

#### Análisis de correlación entre variables continuas

En la primera gráfica se observa una **relación importante entre la altura y el peso** (correlación = 0.46), lo cual es esperable y relevante, ya que ambas variables son fundamentales para determinar si una persona presenta obesidad y en qué grado.  

Asimismo, se aprecia una **relación positiva entre la edad y el peso** (correlación = 0.19), lo que indica que, conforme avanza la edad, el peso tiende a incrementarse debido a distintos factores asociados a cambios en el metabolismo, el estilo de vida y los hábitos de salud.



```{r Correlacion_categoricas,  fig.width=7, fig.height=7, results='hide'}

cat_cols <- names(vars_cat)
cramer_mat <- matrix(NA_real_, length(cat_cols), length(cat_cols),
                     dimnames = list(cat_cols, cat_cols))
for (i in cat_cols) {
  for (j in cat_cols) {
    cramer_mat[i, j] <- suppressWarnings(
      vcd::assocstats(table(vars_cat[[i]], vars_cat[[j]]))$cramer
    )
  }
}
diag(cramer_mat) <- 1

corrplot(
  cramer_mat,
  method        = "circle",
  type          = "upper",
  tl.col        = "black",
  tl.srt        = 45,
  tl.cex        = 0.7,
  addCoef.col   = "black",   # muestra los valores
  number.cex    = 0.6,
  number.digits = 2,
  is.corr       = FALSE,     # escala 0..1 (no -1..1)
  # paleta SECUENCIAL profesional
  col = colorRampPalette(c("#f7fbff", "#c6dbef", "#6baed6", "#2171b5", "#08306b"))(200)
)


```

#### Asociación entre variables categóricas

En el caso de las variables categóricas, se observa que la variable **NObesity** (tipo de obesidad) presenta las asociaciones más relevantes.  
En particular, se encuentra fuertemente relacionada con:  

- **Género**: como se aprecia en las gráficas de barras, cada sexo presenta ciertos tipos de obesidad más predominantes.  
- **FAVC**: frecuencia de consumo de comida con alto contenido calórico.  
- **FCVC**: frecuencia de consumo de verduras.  
- **NCP**: número de comidas principales al día.  
- **Historial familiar con obesidad**.  
- **CAEC**: consumo de alimentos entre comidas principales.  

Estos resultados son consistentes con la literatura, ya que el **sexo**, los **antecedentes familiares** y los **hábitos alimenticios** suelen ser predictores importantes en el desarrollo de sobrepeso y obesidad.  
Por lo tanto, estas variables pueden considerarse como **parámetros clave para identificar y clasificar** si una persona presenta o no obesidad o sobrepeso.



### Análisis de PCA

```{r pca_compute, fig.width=10, fig.height=4}

num_cols <- c("Age","Height","Weight")
stopifnot(all(num_cols %in% names(data)))
X <- scale(data[, num_cols, drop = FALSE], center = TRUE, scale = TRUE)
pca2 <- prcomp(X, center = FALSE, scale. = FALSE)



vars_cat_tmp <- data %>% dplyr::select(where(is.factor)) %>% as.data.frame()
cc <- names(vars_cat_tmp)
cramer_mat <- matrix(NA_real_, length(cc), length(cc), dimnames = list(cc, cc))
for (i in cc) for (j in cc) {
    cramer_mat[i, j] <- suppressWarnings(
      vcd::assocstats(table(vars_cat_tmp[[i]], vars_cat_tmp[[j]]))$cramer
    )
  }
diag(cramer_mat) <- 1
pca <- prcomp(cramer_mat, scale. = TRUE)


```

**Objetivo.** Preparar los objetos `pca` (categóricas, vía matriz de asociación) y `pca2` (continuas), generar los *scree plots* y dejar listos los resultados para las subsecciones posteriores (contribuciones, biplots, clústeres con PCA, etc.).

Con base en el análisis de correlación y asociación realizado previamente, se aplicó la técnica de **Análisis de Componentes Principales (PCA)** con el objetivo de **reducir la dimensionalidad** de los datos y facilitar la interpretación de las variables.



```{r,  fig.width=15, fig.height=4}
p_scree_num <- factoextra::fviz_eig(pca2, addlabels = TRUE) +
  labs(
    title = "Varianza explicada – PCA (continuas)",
    x = "Componentes principales",
    y = "Varianza explicada (%)"
  ) +
  scale_y_continuous(limits = c(0, 100), breaks = seq(0, 100, 10)) +
  theme_minimal(base_size = 12) +
  theme(plot.title = element_text(face = "bold"))

p_scree_cat <- factoextra::fviz_eig(pca, addlabels = TRUE) +
  labs(
    title = "Varianza explicada – PCA (categóricas)",
    x = "Componentes principales",
    y = "Varianza explicada (%)"
  ) +
  scale_y_continuous(limits = c(0, 100), breaks = seq(0, 100, 10)) +
  theme_minimal(base_size = 12) +
  theme(plot.title = element_text(face = "bold"))

(p_scree_cat + p_scree_num) +
  patchwork::plot_annotation(
    title = "Comparación de varianza explicada por PCA",
    subtitle = "Categóricas (Cramér’s V) vs. Continuas (Pearson)",
    tag_levels = "A"
  )

```

En el caso de las **variables numéricas**, el primer componente principal explica un **49.7%** de la varianza total, mientras que el segundo componente aporta un **34.1%** adicional.  
En conjunto, los dos primeros componentes acumulan un **83.71%** de la varianza explicada, lo que permite representar la información de forma compacta y reducir la dimensionalidad de 3 variables a solo 2 componentes.

Por otro lado, el PCA aplicado a las **variables categóricas transformadas** muestra un rendimiento más limitado: los dos primeros componentes explican únicamente un **30.3%** de la varianza acumulada. Incluso considerando un tercer componente, la varianza explicada asciende solo a un **40.9%**, lo que refleja la mayor dispersión e independencia entre estas variables.

En ambos casos, se decidió emplear únicamente **dos componentes principales**, ya que esto permite **visualizar los datos en un plano bidimensional**, identificar posibles **clústeres** y analizar la **contribución de cada variable** a los componentes.




### Contribución de variables a los componentes principales


```{r,  fig.width=15, fig.height=5}
# Gráficas individuales
g1 <- fviz_contrib(pca, choice = "var", axes = 1) + ggtitle("Contribucion al componente 1")
g2 <- fviz_contrib(pca, choice = "var", axes = 2) + ggtitle("Contribucion al componente 2")
g3 <- fviz_contrib(pca, choice = "var", axes = 3) + ggtitle("Contribucion al componente 2")


# Gráficas individuales
g12 <- fviz_contrib(pca2, choice = "var", axes = 1) + ggtitle("Contribucion al componente 1")
g22 <- fviz_contrib(pca2, choice = "var", axes = 2) + ggtitle("Contribucion al componente 2")
g23 <- fviz_contrib(pca2, choice = "var", axes = 3) + ggtitle("Contribucion al componente 3")

# Lado a lado



# Lado a lado
(g1 + g2 + g3) / (g12 + g22 + g23)




```
##### Variables categóricas
- **Componente 1**: Las variables que más aportan son **NObesity** (tipo de obesidad), el **historial familiar con obesidad** y el **género**. Esto indica que el primer eje está fuertemente influido por factores biológicos y antecedentes hereditarios.  
- **Componente 2**: Destacan **FAVC** (consumo de comida con alto contenido calórico), **FCVC** (frecuencia de consumo de verduras), así como **SCC** (control de calorías). Aquí predominan los **hábitos alimenticios** y el manejo de la dieta.  
- **Componente 3**: Tiene mayor peso la variable **MTRANS** (medio de transporte), junto con **NCP** (número de comidas principales) y **TUE** (tiempo frente a pantallas). Este componente refleja más aspectos de **estilo de vida y movilidad**.

##### Variables numéricas
- **Componente 1**: Las variables con mayor contribución son **peso** y **altura**, seguidas por la **edad**. Esto significa que el primer eje resume principalmente las características **antropométricas**.  
- **Componente 2**: Predomina la **edad**, acompañada por un menor aporte de altura y peso. Aquí se concentra la dimensión **temporal**, mostrando cómo el envejecimiento se relaciona con la variabilidad de los datos.  
- **Componente 3**: El peso y la altura vuelven a destacar, con la edad en un nivel secundario, reforzando que estas variables físicas son las más determinantes.

Estos resultados indican que las dimensiones más relevantes en el espacio continuo tienen que ver con la **constitución física** de los participantes y algunos aspectos de **hábitos de vida** relacionados con la salud.  

El **segundo componente** incorpora principalmente la **edad** y el **tiempo frente a dispositivos electrónicos (TUE)**, mostrando la importancia de factores asociados al **estilo de vida moderno**.  

En resumen, los resultados del PCA sugieren que:  
- En las **variables categóricas** predominan los factores familiares y de hábitos alimenticios.  
- En las **variables continuas** destacan las características físicas básicas (Altura y Peso).  




### Cluster con PCA


```{r,  fig.width=6, fig.height=6}
pca_df <- as.data.frame(pca2$x)
pca_df$NObeyesdad <- data$NObeyesdad

scatterplot_pca <- function(df, pca, texto){
  ggplot(df, aes(x = PC1, y = PC2, color = NObeyesdad)) +
    geom_point(size = 3, alpha = 0.7) +
    theme_minimal() +
    labs(title = texto,
         x = paste0("PC1 (", round(summary(pca)$importance[2,1]*100,1), "% varianza)"),
         y = paste0("PC2 (", round(summary(pca)$importance[2,2]*100,1), "% varianza)")) +
    ggsci::scale_color_npg()
}
scatterplot_pca(pca_df, pca2,  "PCA: separación por NObesity (Continuas)")
```


Aunque el PCA y el número de observaciones no permiten una separación completamente clara entre los grupos, se aprecia cierta **tendencia de ordenamiento en el segundo componente (eje Y)**.  

En particular:  
- Los pacientes con **peso insuficiente** y **peso normal** tienden a ubicarse en la parte inferior del gráfico.  
- Conforme ascendemos en el eje Y, se observa un patrón en el que los distintos **tipos y niveles de obesidad** comienzan a ordenarse de manera progresiva.  

Este comportamiento sugiere que los **componentes principales capturan información relevante sobre el grado de obesidad**, aunque la superposición entre grupos indica que no es posible realizar una clasificación perfecta únicamente con estos dos componentes.

### Interpretación del PCA (Biplot)

**Variables continuas**

```{r,  fig.width=6, fig.height=6}
fviz_pca_var(pca2, col.var = "cos2", 
             geom.var = c("arrow", "text"), 
             labelsize = 2, 
             repel = FALSE)
```




 ***Componente 1*** 
 
Este eje está definido principalmente por las variables:  
- **Peso (Weight)**  
- **Altura (Height)**  

Ambas variables tienen cargas altas y positivas, lo que significa que el **Componente 1** refleja la constitución física de los individuos.  

***Componente 2***

El segundo componente está dominado por la variable:  
- **Edad (Age)**  

Esto indica que el **Componente 2**, se enfoca mas en la edad de los individuos



**Variables categóricas**

```{r , fig.width=6, fig.height=6}
fviz_pca_var(pca, col.var = "cos2", 
             geom.var =c("arrow", "text"), 
             labelsize = 2, 
             repel = FALSE) +
  labs(title = "Círculo de correlaciones (PCA)")
```

**Componente 1**

El primer componente está influido principalmente por:  
- **FAVC**: frecuencia de consumo de comida con alto contenido calórico.  
- **FCVC**: frecuencia de consumo de verduras.  
- **CAEC**: consumo de alimentos entre comidas principales.  
- **SCC**: control de la ingesta calórica.  

Este eje refleja la dimensión de **hábitos alimenticios y control dietético**, lo cual es coherente con el hecho de que la obesidad se encuentra fuertemente vinculada a los patrones de alimentación.

**Componente 2**

El segundo componente está definido principalmente por:  
- **Historial familiar con obesidad**.  
- **NObesity** (tipo de obesidad).  
- **MTRANS**: medio de transporte.  
- **TUE**: tiempo frente a dispositivos electrónicos.  

Este eje agrupa variables asociadas tanto a la **predisposición genética/hereditaria** como a los **estilos de vida sedentarios**, dos factores clave en la explicación de los niveles de obesidad.



## TSNe


# TSNe

```{r}
set.seed(42)

# X = covariables (ya tienes `data` con NObeyesdad factor)
X <- data %>% dplyr::select(-NObeyesdad)
X <- model.matrix(~ . - 1, data = X)            # dummies
X <- scale(X)                                   # escalar

# Eliminar filas duplicadas (t-SNE no acepta duplicados)
keep  <- !duplicated(as.data.frame(X))
X_nd  <- X[keep, , drop = FALSE]
y_nd  <- droplevels(data$NObeyesdad[keep])

# Regla de t-SNE: perplexity <= floor((N-1)/3)
n_eff    <- nrow(X_nd)
max_perp <- max(5, floor((n_eff - 1) / 3))
```


```{r  fig.width=10, fig.height=10}
# t-SNE inicial (rápido) — usamos pca=TRUE e initial_dims para ganar velocidad
perp_use <- min(107, max_perp)

tsne_fit <- Rtsne::Rtsne(
  X_nd,
  dims         = 2,
  perplexity   = perp_use,
  eta          = 200,
  theta        = 0.5,                          # más rápido (0.3 es más preciso, pero más lento)
  max_iter     = 1000,                         # menos iteraciones para vista rápida
  verbose      = TRUE,
  check_duplicates = FALSE,
  pca          = TRUE,
  initial_dims = min(50, ncol(X_nd))
)

tsne_df <- as.data.frame(tsne_fit$Y)
names(tsne_df) <- c("tSNE1", "tSNE2")
tsne_df$NObeyesdad <- y_nd

ggplot(tsne_df, aes(tSNE1, tSNE2, colour = NObeyesdad)) +
  geom_point(size = 1.3, alpha = 0.85) +
  ggsci::scale_color_npg(drop = FALSE, name = "Nivel de obesidad") +
  labs(
    title = sprintf("t-SNE inicial (perplexity = %d, eta = 200, theta = 0.5, iter = 1000)", perp_use),
    x = "t-SNE 1", y = "t-SNE 2"
  ) +
  theme_bw()


```

```{r TSNE_grid_perplexity, fig.width=12, fig.height=8, message=FALSE, warning=FALSE}
set.seed(42)



# Límite teórico de perplexity
n_eff    <- nrow(X_nd)
max_perp <- max(5, floor((n_eff - 1) / 3))

perps_grid <- c(10, 20, 30, 40, 50, 70, 90, 110)
perps_grid <- perps_grid[perps_grid <= max_perp]
if (length(perps_grid) == 0) perps_grid <- max_perp

# Embeddings rápidos y estables
emb_list <- lapply(perps_grid, function(pv) {
  Rtsne::Rtsne(
    X_nd,
    dims         = 2,
    is_distance  = FALSE,
    perplexity   = pv,
    eta          = 200,
    theta        = 0.5,            # más rápido
    max_iter     = 750,            # menos iteraciones para malla
    verbose      = FALSE,
    pca          = TRUE,
    initial_dims = min(50, ncol(X_nd))
  )$Y
})

# Data largo
d <- data.table::rbindlist(lapply(seq_along(emb_list), function(i) {
  data.table::data.table(
    tSNE1      = emb_list[[i]][, 1],
    tSNE2      = emb_list[[i]][, 2],
    perplexity = factor(perps_grid[i], levels = perps_grid),
    group      = y_nd
  )
}))

# Muestra "dummy" para leyenda bonita (sin pintar en panel)
set.seed(42)
idx_legend <- sample.int(nrow(d), size = min(1200, nrow(d)))

ggplot2::ggplot(d, ggplot2::aes(tSNE1, tSNE2, colour = group)) +
  scattermore::geom_scattermore(pointsize = 1.8, alpha = 0.9, show.legend = FALSE) +
  ggplot2::geom_point(data = d[idx_legend, ], alpha = 0, size = 3, show.legend = TRUE) +
  ggsci::scale_color_npg(drop = FALSE, name = "Nivel de obesidad") +
  ggplot2::facet_wrap(~ perplexity, ncol = 3, labeller = ggplot2::label_both) +
  ggplot2::coord_equal() +
  ggplot2::theme_bw(base_size = 12) +
  ggplot2::theme(
    legend.position   = "right",
    legend.title      = ggplot2::element_text(size = 11),
    legend.text       = ggplot2::element_text(size = 10),
    legend.key.width  = grid::unit(1.4, "lines"),
    legend.key.height = grid::unit(1.4, "lines"),
    strip.text        = ggplot2::element_text(face = "bold"),
    panel.grid.minor  = ggplot2::element_blank(),
    panel.spacing     = grid::unit(0.9, "lines")
  ) +
  ggplot2::guides(
    colour = ggplot2::guide_legend(
      override.aes = list(size = 6, alpha = 1, shape = 16, stroke = 0)
    )
  ) +
  ggplot2::labs(
    title = "t-SNE (rejilla para perplexity)",
    x = "tSNE1", y = "tSNE2"
  )
```



```{r TSNE_grid_perp_eta, fig.width=12, fig.height=8, message=FALSE, warning=FALSE}
set.seed(42)

y_nd <- factor(
  y_nd,
  levels = c("Insufficient_Weight","Normal_Weight",
             "Obesity_Type_I","Obesity_Type_II","Obesity_Type_III",
             "Overweight_Level_I","Overweight_Level_II")
)

# Límite teórico
n_eff    <- nrow(X_nd)
max_perp <- max(5, floor((n_eff - 1) / 3))

perps <- c(10, 30, 50, 70)
perps <- perps[perps <= max_perp]
if (length(perps) == 0) perps <- max_perp
etas  <- c(50, 200)

grid <- expand.grid(perplexity = perps, eta = etas)

emb_list2 <- lapply(seq_len(nrow(grid)), function(i) {
  Rtsne::Rtsne(
    X_nd,
    dims         = 2,
    is_distance  = FALSE,
    perplexity   = grid$perplexity[i],
    eta          = grid$eta[i],
    theta        = 0.5,
    max_iter     = 750,
    verbose      = FALSE,
    pca          = TRUE,
    initial_dims = min(50, ncol(X_nd))
  )$Y
})

d2 <- data.table::rbindlist(lapply(seq_len(nrow(grid)), function(i) {
  data.table::data.table(
    tSNE1      = emb_list2[[i]][, 1],
    tSNE2      = emb_list2[[i]][, 2],
    perplexity = factor(grid$perplexity[i], levels = perps),
    eta        = factor(grid$eta[i],        levels = etas),
    group      = y_nd
  )
}))

set.seed(42)
idx_legend2 <- sample.int(nrow(d2), size = min(1200, nrow(d2)))

ggplot2::ggplot(d2, ggplot2::aes(tSNE1, tSNE2, colour = group)) +
  scattermore::geom_scattermore(pointsize = 1.8, alpha = 0.9, show.legend = FALSE) +
  ggplot2::geom_point(data = d2[idx_legend2, ], alpha = 0, size = 3, show.legend = TRUE) +
  ggsci::scale_color_npg(drop = FALSE, name = "Nivel de obesidad") +
  ggplot2::facet_grid(eta ~ perplexity, labeller = ggplot2::label_both) +
  ggplot2::coord_equal() +
  ggplot2::theme_bw(base_size = 12) +
  ggplot2::theme(
    legend.position   = "right",
    legend.title      = ggplot2::element_text(size = 11),
    legend.text       = ggplot2::element_text(size = 10),
    legend.key.width  = grid::unit(1.4, "lines"),
    legend.key.height = grid::unit(1.4, "lines"),
    strip.text        = ggplot2::element_text(face = "bold"),
    panel.grid.minor  = ggplot2::element_blank(),
    panel.spacing     = ggplot2::unit(0.9, "lines")
  ) +
  ggplot2::guides(
    colour = ggplot2::guide_legend(
      override.aes = list(size = 6, alpha = 1, shape = 16, stroke = 0)
    )
  ) +
  ggplot2::labs(
    title = "t-SNE (rejilla de combinaciones perplexity y eta)",
    x = "tSNE1", y = "tSNE2"
  )
```


```{r,  fig.width=10, fig.height=10}

# --- helpers ---
knn_overlap <- function(Xh, Yh, k = 15, n_eval = 1500) {
  n  <- nrow(Xh)
  if (n <= k + 1) return(NA_real_)
  idx <- if (n > n_eval) sample.int(n, n_eval) else seq_len(n)

  # K vecinos en alto-dim (X) y bajo-dim (Y)
  nnX <- RANN::nn2(Xh, query = Xh[idx, , drop = FALSE], k = k + 1)$nn.idx[, -1, drop = FALSE]
  nnY <- RANN::nn2(Yh, query = Yh[idx, , drop = FALSE], k = k + 1)$nn.idx[, -1, drop = FALSE]

  # solape medio por fila
  mean(vapply(seq_len(nrow(nnX)), function(i) {
    length(intersect(nnX[i, ], nnY[i, ])) / k
  }, numeric(1)))
}

silhouette_supervised <- function(Yh, labels, n_eval = 2000) {
  n  <- nrow(Yh)
  if (length(unique(labels)) < 2) return(NA_real_)
  idx <- if (n > n_eval) sample.int(n, n_eval) else seq_len(n)
  d   <- dist(Yh[idx, , drop = FALSE])
  cl  <- as.integer(labels[idx])
  sil <- cluster::silhouette(cl, d)[, 3]
  mean(sil, na.rm = TRUE)
}

# --- cuadrícula de evaluación (compacta para tiempo razonable) ---
grid_m <- expand.grid(
  perplexity = pmin(c(10, 20, 30, 40, 50, 70), max_perp),
  eta        = c(50, 200)
)

eval_list <- lapply(seq_len(nrow(grid_m)), function(i) {
  out <- Rtsne::Rtsne(
    X_nd,
    dims         = 2,
    perplexity   = grid_m$perplexity[i],
    eta          = grid_m$eta[i],
    theta        = 0.5,
    max_iter     = 750,
    verbose      = FALSE,
    check_duplicates = FALSE,
    pca          = TRUE,
    initial_dims = min(50, ncol(X_nd))
  )
  Y   <- out$Y
  sil <- silhouette_supervised(Y, y_nd, n_eval = 2000)
  knn <- knn_overlap(X_nd, Y, k = 15, n_eval = 1500)
  data.frame(perplexity = grid_m$perplexity[i],
             eta        = grid_m$eta[i],
             silhouette = sil,
             knn_overlap= knn)
})

metrics <- dplyr::bind_rows(eval_list) %>%
  mutate(
    z_sil = as.numeric(scale(silhouette)),
    z_knn = as.numeric(scale(knn_overlap)),
    score = z_sil + z_knn
  ) %>%
  arrange(desc(score))

# Tabla LaTeX (top 10)
knitr::kable(head(metrics, 10),
             format = "latex", booktabs = TRUE, digits = 3,
             col.names = c("Perplexity","Eta","Silhouette","KNN overlap","z(Sil.)","z(KNN)","Score")) %>%
  kableExtra::kable_styling(latex_options = c("hold_position"))

# Entrenamos "mejor" con más calidad (más iter y theta menor)
best_perp <- metrics$perplexity[1]
best_eta  <- metrics$eta[1]

tsne_best <- Rtsne::Rtsne(
  X_nd,
  dims         = 2,
  perplexity   = best_perp,
  eta          = best_eta,
  theta        = 0,                     # un poco más preciso
  max_iter     = 3000,                    # más calidad en el mejor
  verbose      = TRUE,
  check_duplicates = FALSE,
  pca          = TRUE,
  initial_dims = min(50, ncol(X_nd))
)

df_best <- as.data.frame(tsne_best$Y)
colnames(df_best) <- c("tSNE1","tSNE2")
df_best$NObeyesdad <- factor(
  y_nd,
  levels = c("Insufficient_Weight","Normal_Weight",
             "Overweight_Level_I","Overweight_Level_II",
             "Obesity_Type_I","Obesity_Type_II","Obesity_Type_III")
)

ggplot(df_best, aes(tSNE1, tSNE2, colour = NObeyesdad)) +
  scattermore::geom_scattermore(pointsize = 2) +
  ggsci::scale_color_npg(name = "Nivel de obesidad", drop = FALSE) +
  theme_minimal(base_size = 11) +
  theme(
    legend.position  = "bottom",
    panel.grid.minor = element_blank()
  ) +
  labs(
    title = sprintf("t-SNE seleccionado (perplexity = %d, eta = %d)", best_perp, best_eta),
    x = "t-SNE 1", y = "t-SNE 2"
  )
```

```{r}
perps <- c(5, 10, 15, 20, 30, 40, 50, 60, 80, 100, 120, 140, 150)
perps <- perps[perps <= max_perp]

out_dir <- "FiguresTSNE"
if (!dir.exists(out_dir)) dir.create(out_dir, recursive = TRUE)

.plot_tsne <- function(perp, eta = 200, seed = 42) {
  set.seed(seed)
  ts <- Rtsne(
    X_nd,
    dims         = 2,
    perplexity   = perp,
    eta          = eta,
    theta        = 0.5,
    max_iter     = 1000,
    verbose      = FALSE,
    pca          = TRUE,
    initial_dims = min(50, ncol(X_nd))
  )
  dfp <- as.data.frame(ts$Y)
  colnames(dfp) <- c("tSNE1","tSNE2")
  dfp$NObeyesdad <- factor(
    y_nd,
    levels = c("Insufficient_Weight","Normal_Weight",
               "Overweight_Level_I","Overweight_Level_II",
               "Obesity_Type_I","Obesity_Type_II","Obesity_Type_III")
  )
  ggplot(dfp, aes(tSNE1, tSNE2, colour = NObeyesdad)) +
    scattermore::geom_scattermore(pointsize = 2) +
    ggsci::scale_color_npg(name = "Nivel de obesidad", drop = FALSE) +
    theme_minimal(base_size = 10) +
    theme(
      legend.position  = "bottom",
      panel.grid.minor = element_blank()
    ) +
    labs(title = sprintf("t-SNE (perplexity = %d, eta = %d)", perp, eta),
         x = "t-SNE 1", y = "t-SNE 2")
}

for (pp in perps) {
  p <- .plot_tsne(pp, eta = 200)
  ggsave(file.path(out_dir, sprintf("TSNE_perp_%d.png", pp)),
         plot = p, width = 7, height = 6, dpi = 300)
}
```

# UMAP

Escalando los datos de las variables numericas para emplear UMAP e indicando el valor del centroide
```{r}
# UMAP sobre numéricas estandarizadas
set.seed(123)

X_num <- data %>%
  dplyr::select(where(is.numeric)) %>%
  scale() %>%
  as.matrix()

data_umap <- uwot::umap(
  X_num
)

str(data_umap)


```


Se grafican los dos primeros ejes El segundo eje separa al conjunto de sujetos con Obesidad tipo III de los de la Obesidad tipo I, que visulamente, estan ensimados sobre los de Obesidad tipo II. Las categorías restantes se encuentran mezcladas no permitiendo ver un patron definido.
```{r}
# Construir data frame para graficar a partir del objeto creado: data_umap


umap_df <- tibble::tibble(
  UMAP1 = data_umap[, 1],
  UMAP2 = data_umap[, 2],
  NObeyesdad = data$NObeyesdad
)

head(umap_df)


ggplot(umap_df, aes(x = UMAP1, y = UMAP2, color = NObeyesdad)) +
  geom_point(size = 1.8, alpha = 0.85) +
  ggsci::scale_color_npg(name = "Nivel de obesidad") +  # opcional
  labs(title = "Obesidad: UMAP", x = "UMAP1", y = "UMAP2", color = "Nivel de obesidad") +
  theme_minimal() +
  theme(legend.position = "bottom")


```

Empleamos 
```{r}

set.seed(123)

umap_rec <- recipe(NObeyesdad ~ ., data = data) %>%
  step_dummy(all_nominal_predictors(), one_hot = TRUE) %>%
  step_zv(all_predictors()) %>%
  step_normalize(all_numeric_predictors()) %>%
  embed::step_umap(
    all_predictors()
  )

umap_res <- prep(umap_rec)


umap_juice <- juice(umap_res)

ggplot(umap_juice, aes(UMAP1, UMAP2, color = NObeyesdad)) +
  geom_point(size = 1.6, alpha = 0.9) +
  labs(title = "UMAP (recipes + embed)", x = "UMAP1", y = "UMAP2", color = "Nivel de obesidad") +
  theme_minimal() +
  theme(legend.position = "bottom")


```



Exploración con Hiperparámetros

En clase se hizo enfasis en que UMAP es mejor realizarlo con definición de hiperparámteros, en los cuales se hace referencia que los más importantes son las vecinos más cercano y la distancia mínima. No se establació una regla para definir dichos parámetros, sin embargo después de realizar varias pruebas se llegaron a los siguientes:
   - Vecinos más cercanos sugerimos grupos de 10, 20, 40 y 80 
   - Distancias 0.5, 0.75, 1. 

Las mejores gráficas en cuanto a vecinos más cercanos es para 80 y la de mínima distancia es la de 0.5. 

Es importante decir que el orden de la escala térmica en las gráficas indica se evidencia el orden de los niveles de obesidad, es decir, el peso insuficiente, peso normal, sobrepeso nivel I, sobrepeso nivel II, obesidad grado I, obesidad grado II  y la obesidad grado III, lo que la técinica de reducción de dimensiones con UMAP es adecuada.
```{r}
# UMAP – Rejilla de hiperparámetros (n_neighbors × min_dist) con formato profesional
set.seed(123)

if (!exists("X_num")) {
  X_num <- data %>%
    dplyr::select(where(is.numeric)) %>%
    scale() %>%
    as.matrix()
}

grid <- expand.grid(
  n_neighbors = c(10, 20, 40, 80, 120),
  min_dist    = c(0.25, 0.50, 0.75, 1.00)
)

emb_list <- lapply(seq_len(nrow(grid)), function(i) {
  uwot::umap(
    X_num,
    n_neighbors  = grid$n_neighbors[i],
    min_dist     = grid$min_dist[i]
  )
})

d <- data.table::rbindlist(lapply(seq_along(emb_list), function(i) {
  data.table::data.table(
    UMAP1 = emb_list[[i]][, 1],
    UMAP2 = emb_list[[i]][, 2],
    n_neighbors = factor(grid$n_neighbors[i], levels = sort(unique(grid$n_neighbors))),
    min_dist    = factor(grid$min_dist[i],    levels = sort(unique(grid$min_dist))),
    NObeyesdad  = data$NObeyesdad
  )
}))

p <- ggplot(d, aes(UMAP1, UMAP2, color = NObeyesdad)) +
  geom_point(size = 0.6, alpha = 0.6) +
  facet_grid(min_dist ~ n_neighbors, labeller = label_both) +
  ggsci::scale_color_npg(name = "Nivel de obesidad", drop = FALSE) +
  coord_equal() +
  labs(
    title    = "UMAP (rejilla de combinaciones para n_neighbors y min_dist)",
    x = "UMAP1", y = "UMAP2"
  ) +
  theme_bw(base_size = 9) +
  theme(
    panel.grid.minor   = element_blank(),
    strip.background   = element_rect(fill = "grey92", colour = NA),
    legend.position    = "right",
    legend.key.height  = grid::unit(0.7, "cm"),
    legend.key.width   = grid::unit(0.7, "cm"),
    legend.title       = element_text(size = 10),
    legend.text        = element_text(size = 9)
  ) +
  guides(color = guide_legend(override.aes = list(size = 4, alpha = 1)))

p



```

Con otros valores de mayor número de vecinos más cercanos y menor distancia que el anterior. Para este ajuste, las gráficas con distancia de 0.25 resultaron mejor definidas con respecto a los tres valores de vecinos más cercano, lo que nos indica que, para esta modelo, la distancia es más importante que la cantidad de vecinos más cercanos.
```{r}
# --- Grid 2: n_neighbors = 50,100,150 × min_dist = 0.25,0.75,1.25
set.seed(123)
grid2 <- expand.grid(n_neighbors = c(50, 100, 150),
                     min_dist    = c(0.25, 0.75,1.0, 1.25))

umaps2 <- lapply(seq_len(nrow(grid2)), function(i) {
  uwot::umap(X_num,
             n_neighbors = grid2$n_neighbors[i],
             min_dist    = grid2$min_dist[i])
})

d_grid2 <- data.table::rbindlist(lapply(seq_len(nrow(grid2)), function(i) {
  data.table::data.table(
    UMAP1 = umaps2[[i]][,1],
    UMAP2 = umaps2[[i]][,2],
    n_neighbors = factor(grid2$n_neighbors[i]),
    min_dist    = factor(grid2$min_dist[i]),
    NObeyesdad  = data$NObeyesdad
  )
}))

p_grid2 <- ggplot(d_grid2, aes(UMAP1, UMAP2, color = NObeyesdad)) +
  geom_point(size = 0.6, alpha = 0.6) +
  facet_grid(min_dist ~ n_neighbors, labeller = label_both) +
  ggsci::scale_color_npg(name = "Nivel de obesidad", drop = FALSE) +
  coord_equal() +
  labs(title = "UMAP (rejilla para n_neighbors y min_dist)",
       x = "UMAP1", y = "UMAP2") +
  theme_bw(base_size = 9) +
  theme(panel.grid.minor = element_blank(),
        strip.background = element_rect(fill = "grey92", colour = NA),
        legend.position = "right",
        legend.key.height = grid::unit(0.7, "cm"),
        legend.key.width  = grid::unit(0.7, "cm"),
        legend.title = element_text(size = 10),
        legend.text  = element_text(size = 9)) +
  guides(color = guide_legend(override.aes = list(size = 4, alpha = 1)))
p_grid2


```



Se hizo la animación con los parámetros que se consideraron mejores con respect a las distancias de 0.25 y 0.5 y los vecinos más cercanos de 10, 20, 40, 50.  
```{r}
# Animación UMAP – verificación de hiperparámetros (n_neighbors ∈ {10,20,40,50}, min_dist ∈ {0.25, 0.50})


set.seed(123)


grid_anim <- expand.grid(
  n_neighbors = c(10, 20, 40, 50),
  min_dist    = c(0.25, 0.50)
)

emb_anim <- lapply(seq_len(nrow(grid_anim)), function(i) {
  uwot::umap(
    X_num,
    n_neighbors  = grid_anim$n_neighbors[i],
    min_dist     = grid_anim$min_dist[i]
  )
})

anim_df <- data.table::rbindlist(lapply(seq_along(emb_anim), function(i) {
  data.table::data.table(
    UMAP1 = emb_anim[[i]][, 1],
    UMAP2 = emb_anim[[i]][, 2],
    parametros = sprintf("n_neighbors: %s — min_dist: %s",
                         grid_anim$n_neighbors[i], grid_anim$min_dist[i]),
    NObeyesdad = data$NObeyesdad
  )
}))
anim_df$parametros <- factor(anim_df$parametros, levels = unique(anim_df$parametros))

anim_plot1 <- ggplot(anim_df, aes(UMAP1, UMAP2, colour = NObeyesdad)) +
  geom_point(size = 0.8, alpha = 0.7) +
  ggsci::scale_color_npg(name = "Nivel de obesidad", drop = FALSE) +
  coord_equal() +
  labs(
    title    = "UMAP – verificación de hiperparámetros",
    subtitle = "{closest_state}",
    x = NULL, y = NULL
  ) +
  theme_bw(base_size = 10) +
  theme(
    panel.grid.minor  = element_blank(),
    axis.text         = element_blank(),
    axis.ticks        = element_blank(),
    legend.position   = "right",
    legend.key.height = grid::unit(0.7, "cm"),
    legend.key.width  = grid::unit(0.7, "cm")
  ) +
  guides(colour = guide_legend(override.aes = list(size = 4, alpha = 1))) +
  transition_states(parametros, transition_length = 2, state_length = 3)

# Render 
animate(anim_plot1, nframes = 300, fps = 25, width = 900, height = 600,renderer = av_renderer("umap_anim.mp4"))


```


```{r}

d1_anim <- d1
if (!all(c("x","y") %in% names(d1_anim)) && all(c("UMAP1","UMAP2") %in% names(d1_anim))) {
  d1_anim <- d1_anim %>% dplyr::rename(x = UMAP1, y = UMAP2)
}


anim_plot2 <- d1_anim %>% 
  dplyr::filter(min_dist %in% c(0.5, 0.75, 1.1)) %>% 
  dplyr::mutate(parametros = factor(paste('n_neighbors:', n_neighbors, 'min_dist:', min_dist))) %>% 
  ggplot2::ggplot(ggplot2::aes(x = x, y = y, col = group)) +
  scattermore::geom_scattermore(pointsize = 2) +
  ggplot2::theme_bw() +
  ggplot2::labs(
    title    = 'Selección de hiperparámetros (UMAP)'
  ) +
  ggplot2::theme(
    axis.text  = ggplot2::element_blank(),
    axis.ticks = ggplot2::element_blank(),
    axis.title = ggplot2::element_blank(),
    legend.position = "none"
  ) +
  gganimate::transition_states(parametros, transition_length = 2, state_length = 3)



mp4_file <- "umap_anim_minDist_050_075_110.mp4"
gganimate::animate(
  anim_plot2,
  renderer = av_renderer(mp4_file),
  width = 900, height = 600, res = 120,
  fps = 30, duration = 12  
)



```

```{r}


set.seed(123)


X_num <- data %>%
  dplyr::select(where(is.numeric)) %>%
  scale() %>%
  as.matrix()

y <- droplevels(data$NObeyesdad)


grid_fig <- dplyr::bind_rows(
  expand.grid(n_neighbors = c(10, 20, 40, 80, 120), min_dist = c(0.25, 0.50, 0.75, 1.00)),
  expand.grid(n_neighbors = c(50, 100, 150),        min_dist = c(0.25, 0.75, 1.0, 1.25))
) |> dplyr::distinct()

# Embeddings UMAP 
emb_list <- lapply(seq_len(nrow(grid_fig)), function(i) {
  uwot::umap(
    X          = X_num,
    n_neighbors  = grid_fig$n_neighbors[i],
    min_dist     = grid_fig$min_dist[i]
  )
})

# Métricas: silhouette por clase + solapamiento de vecinos (KNN overlap)
eval_one <- function(Y, y, k = 15) {
  idx_X <- FNN::get.knn(X_num, k = k)$nn.index
  idx_Y <- FNN::get.knn(Y,     k = k)$nn.index
  knn_overlap <- mean(vapply(seq_len(nrow(X_num)), function(r) {
    mean(idx_Y[r, ] %in% idx_X[r, ])
  }, numeric(1)))

  sil <- tryCatch({
    s <- cluster::silhouette(as.integer(y), stats::dist(Y))
    mean(s[, 3], na.rm = TRUE)
  }, error = function(e) NA_real_)

  c(silhouette = sil, knn_overlap = knn_overlap)
}

metrics_fig <- do.call(rbind, lapply(seq_len(nrow(grid_fig)), function(i) {
  m <- eval_one(emb_list[[i]], y, k = 15)
  data.frame(
    n_neighbors = grid_fig$n_neighbors[i],
    min_dist    = grid_fig$min_dist[i],
    silhouette  = unname(m["silhouette"]),
    knn_overlap = unname(m["knn_overlap"])
  )
})) |>
  mutate(
    z_sil = as.numeric(scale(silhouette)),
    z_knn = as.numeric(scale(knn_overlap)),
    score = z_sil + z_knn
  ) |>
  arrange(desc(score))


print(head(metrics_fig, 10))


knitr::kable(
  head(metrics_fig, 12) |>
    mutate(across(c(silhouette, knn_overlap, score), ~ round(.x, 3))),
  format   = "latex", booktabs = TRUE,
  col.names = c("$n_{neighbors}$","$min\\_dist$","Silhouette","KNN overlap","z(Sil.)","z(KNN)","Score")
)

# Toma la mejor combinación y se grafica
best_cfg <- metrics_fig |> slice(1)
best_Y <- uwot::umap(
  X_num,
  n_neighbors  = best_cfg$n_neighbors,
  min_dist     = best_cfg$min_dist
)

df_best <- data.frame(
  UMAP1 = best_Y[, 1],
  UMAP2 = best_Y[, 2],
  NObeyesdad = y
)

ggplot(df_best, aes(UMAP1, UMAP2, colour = NObeyesdad)) +
  geom_point(alpha = 0.85, size = 1.6) +
  ggsci::scale_color_npg(
    name = "Nivel de obesidad",
    guide = guide_legend(override.aes = list(size = 5))
  ) +
  theme_minimal() +
  labs(
    title = sprintf("UMAP seleccionado: los mejores parámetros son n_neighbors=%s, min_dist=%s",
                    best_cfg$n_neighbors, best_cfg$min_dist),
    x = "UMAP1", y = "UMAP2"
  )

```


```{r}

plot_umap_combo <- function(nn = 20, md = 0.25, metric = "euclidean", seed = 123) {
  set.seed(seed)
  emb <- uwot::umap(
    X          = X_num,
    n_neighbors  = nn,
    min_dist     = md,
    metric       = metric,
    n_components = 2,
    n_threads    = 1
  )
  df_plot <- data.frame(
    UMAP1 = emb[, 1],
    UMAP2 = emb[, 2],
    NObeyesdad = data$NObeyesdad
  )

  ggplot2::ggplot(df_plot, ggplot2::aes(UMAP1, UMAP2, colour = NObeyesdad)) +
    ggplot2::geom_point(size = 1.8, alpha = 0.85) +
    ggsci::scale_color_npg(name = "Nivel de obesidad", drop = FALSE) +
    ggplot2::guides(color = ggplot2::guide_legend(override.aes = list(size = 4))) +
    ggplot2::theme_minimal(base_size = 12) +
    ggplot2::theme(
      legend.position = "bottom",
      legend.box      = "horizontal"
    ) +
    ggplot2::labs(
      title = sprintf("UMAP para n_neighbors = %s, min_dist = %.2f", nn, md),
      x = "UMAP1", y = "UMAP2"
    )
}

p_best <- plot_umap_combo(nn = 150, md = 0.25)
p_best

```















