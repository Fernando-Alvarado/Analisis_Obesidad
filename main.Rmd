---
title: "PCA y FA de Phishing"
output: html_document
---



```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  message = FALSE,
	warning = FALSE,
	error = F
)

#Ruta de los datos en caso de que requiera

#Cargamos nuestras librerías 
library(ggplot2)
library(dplyr)
#Librerias necesarias
library(tidyverse)
library(ggplot2)
library(factoextra)
library(dplyr)
library(esquisse)
library(scatterplot3d)
library(factoextra)
library(patchwork)
library(here)
library(esquisse)
library(skimr)
library(corrplot)
library(vcd)
library(Rtsne)

```

```{r Data}

data <- read.csv(here("Data", "Obesity_clean.csv"))


data$FCVC <- as.factor(data$FCVC)
data$NCP  <- as.factor(data$NCP)
data$CH2O <- as.factor(data$CH2O)
data$FAF  <- as.factor(data$FAF)
data$TUE  <- as.factor(data$TUE)




```

## Introducción a los Datos

El conjunto de datos **Obesity.csv** contiene información destinada a estimar los niveles de obesidad en función de los hábitos alimentarios, los antecedentes familiares y la condición física de los individuos. La base de datos incluye registros de personas de México, Perú y Colombia, abarcando un total de **2,111 observaciones** y **16 variables** relacionadas con el estilo de vida y la salud.

Las variables recogen información como el género, edad, altura, peso, antecedentes familiares de sobrepeso, frecuencia de consumo de ciertos alimentos, hábitos de hidratación, control de la ingesta calórica, actividad física, tiempo de uso de tecnología, consumo de alcohol y medio de transporte habitual. Además, se incluyen etiquetas que clasifican el nivel de obesidad, desde peso insuficiente hasta obesidad tipo III.

La mayoría de los datos fueron generados mediante técnicas sintéticas, mientras que algunos provienen directamente de usuarios a través de una plataforma web, lo que permite contar con un conjunto diverso y representativo para el análisis.



```{r}
#skim(data)

analisis <- data 


skim(analisis)
```
### Variables seleccionadas

- **FCVC**: Indica la frecuencia con la que el individuo consume verduras.  
  Escalas: 1 = **Nunca**, 2 = **A veces**, 3 = **Siempre**.  

- **NCP**: Representa el número de comidas principales consumidas por día.  
  Escalas: 1 = **1–2 comidas**, 2 = **3 comidas**, 3 = **4 comidas**, 4 = **Más de 4 comidas**.  

- **CH2O**: Describe la cantidad de agua consumida diariamente.  
  Escalas: 1 = **Menos de 1L**, 2 = **1–2L**, 3 = **Más de 2L**.  

- **FAF**: Señala la frecuencia con la que la persona realiza actividad física.  
  Escalas: 0 = **Nunca**, 1 = **1–2 veces por semana**, 2 = **2–4 veces por semana**, 3 = **4–5 veces por semana**.  

- **TUE**: Cuantifica el número de horas al día frente a un dispositivo electrónico.  
  Escalas: 0 = **0–2 horas**, 1 = **3–5 horas**, 2 = **Más de 5 horas**.  


### Estadísticas descriptivas

En este estudio, los participantes presentaron en promedio:  

- **Edad**: 24 años  
- **Estatura**: 1.70 metros  
- **Peso**: 86.5 kilogramos  
- **Comidas principales**: Entre 3 y 4 por día  
- **Consumo de verduras**: Ocasional  
- **Consumo de agua**: 1–2 litros diarios  
- **Actividad física**: 1–2 veces por semana  
- **Uso de dispositivos electrónicos**: Entre 2 y 5 horas diarias  

A primera vista, estos resultados sugieren que los participantes no mantienen los **mejores hábitos de salud**, dado el consumo moderado de verduras y agua, la baja frecuencia de actividad física y la exposición considerable al uso de dispositivos electrónicos.


```{r}
data <- data %>%
  mutate(Gender = as.factor(Gender),
         CALC = as.factor(CALC),
         FAVC = as.factor(FAVC),
         SCC = as.factor(SCC),
         SMOKE = as.factor(SMOKE),
         family_history_with_overweight = as.factor(family_history_with_overweight),
         CAEC = as.factor(CAEC),
         MTRANS = as.factor(MTRANS),
         NObeyesdad = as.factor(NObeyesdad)
         
         
  )
```


Resumen de los datos....


### Dividiendo los datos en continuos y en categoricos

```{r}
# Variables categóricas (factores o cadenas)
var_numeric <- data %>% dplyr::select(where(is.numeric), NObeyesdad) 


vars_cat <- data %>%  dplyr::select(where(is.factor))


summary(vars_cat)

```

```{r Graficas_Bonitas1,  fig.width=15, fig.height=17}
entradas <- c("Gender" ,"CALC", "FAVC", "SCC"  ,"SMOKE" ,"family_history_with_overweight" ,"CAEC" ,"MTRANS", "NObeyesdad", "FCVC", "NCP", "CH2O", "FAF", "TUE" ) #Agregue las que faltaban, solo es cuestion de limpiar las variables
graficasCat <- list()


for (i in entradas) {
  print(i)
 p <- ggplot(data, aes_string(x = i, fill = "NObeyesdad")) +
  geom_bar(position = "fill") +
  geom_text(
     stat = "count",
    aes(label = scales::percent(..count..), group = NObeyesdad),
    position = position_fill(vjust = 0.5),
    size = 3
  ) +
  scale_fill_manual(values = c(
    Insufficient_Weight = "#03BCE7",
    Normal_Weight      = "#58D71D",
    Obesity_Type_I     = "#FFDB6C",
    Obesity_Type_II    = "#FFA500",
    Obesity_Type_III   = "#FF0000",
    Overweight_Level_I = "#C9082A",
    Overweight_Level_II= "#800026"
  )) +
  labs(title = paste(i, "vs NObesity")) +
  theme_minimal() +
  theme(
    axis.text.x  = element_text(angle = 45, hjust = 1),
    legend.text  = element_text(size = 7),
    legend.title = element_text(size = 10),
    legend.key.size = unit(0.4, "cm")
  )
  
  # Guardar la gráfica en la lista
  graficasCat[[length(graficasCat) + 1]] <- p
}



#  Aqui se suman las gráficas, para imprimirlas juntas
graficasCat[[1]] + graficasCat[[2]] + graficasCat[[3]] + graficasCat[[4]] + graficasCat[[5]] + graficasCat[[6]] + graficasCat[[7]] + graficasCat[[8]] + graficasCat[[9]] +  graficasCat[[10]] + graficasCat[[11]] + graficasCat[[12]] + graficasCat[[13]] + graficasCat[[14]] + plot_layout(ncol = 3)


```
En la siguiente gráfica se observa que nuestras clases están **balanceadas**, es decir, contamos con un número similar de participantes en cada categoría de **Grado de Obesidad**, lo cual se mantiene de manera consistente a lo largo de todas las variables analizadas.



```{r, Graficas_Bonitas2,  fig.width=15, fig.height=4}
vars <- c("Age", "Height", "Weight")
graficasNum <- list()

for(i in vars){
  p <- ggplot(var_numeric) +
  aes_string(x = i, fill = "NObeyesdad") +
  geom_histogram(bins = 30L) +
  scale_fill_manual(
    values = c(Insufficient_Weight = "#03BCB7",
    Normal_Weight = "#58D713",
    Obesity_Type_I = "#FFDB0C",
    Obesity_Type_II = "#FF8600",
    Obesity_Type_III = "#F44408",
    Overweight_Level_I = "#C90822",
    Overweight_Level_II = "#800026")
  ) +
  labs(title = paste0(i, " vs NObesity"), x = i) +
  theme_minimal() +
  theme(
    legend.text = element_text(size = 7),     # Tamaño texto
    legend.title = element_text(size = 8),    # Tamaño título
    legend.key.size = unit(0.4, "cm")         # Tamaño recuadros
  )
  
  graficasNum[[length(graficasNum) + 1]] <- p
}
graficasNum[[1]] + graficasNum[[2]] + graficasNum[[3]]  + plot_layout(ncol = 3)


```

En la siguiente gráfica se muestra la distribución de nuestras variables.  
En particular, observamos que la mayoría de los participantes en el estudio son **jóvenes entre 20 y 24 años**.  
Además, se aprecia que la estatura más frecuente se encuentra en el rango de **1.60 a 1.80 metros**, lo que corresponde al promedio esperado en la población analizada.



### Análisis de correlación

Para este análisis se aplicaron dos enfoques complementarios:

- **Variables continuas**: se calcularon coeficientes de correlación de Pearson, lo que permite medir la relación lineal entre dos variables cuantitativas.  
- **Variables categóricas**: se utilizó el **Coeficiente V de Cramer**, basado en el estadístico Chi-cuadrado. Este coeficiente es apropiado para medir la **fuerza de asociación** entre variables cualitativas y toma valores entre 0 (sin asociación) y 1 (asociación perfecta).

El uso de ambos métodos cuenta con sustento teórico: la correlación de Pearson es un estándar ampliamente aceptado para datos continuos, mientras que el V de Cramer es una extensión del Chi-cuadrado que evita problemas de dependencia con el tamaño de la muestra, lo que lo hace adecuado para el análisis de variables categóricas.

De esta forma, se obtiene una visión más completa de las posibles relaciones entre las variables de estudio, considerando tanto las continuas como las categóricas.

```{r Correlacion_Continueas}

var_numeric <- var_numeric %>% dplyr::select(-NObeyesdad )

cor(var_numeric)

corrplot(cor(var_numeric), method = "circle", type = "upper", tl.col = "black", tl.srt = 45, addCoef.col = "black")
```

#### Análisis de correlación entre variables continuas

En la primera gráfica se observa una **relación importante entre la altura y el peso**  
(correlación = 0.46), lo cual es esperable y relevante, ya que ambas variables son claves para determinar si una persona presenta obesidad y en qué grado.  

También se aprecia una **relación negativa entre la edad y el número de horas frente a un dispositivo electrónico** (correlación = -0.30), lo que sugiere que los participantes más jóvenes tienden a pasar más tiempo utilizando dispositivos electrónicos.  

Otras correlaciones de interés son:  
- **Altura y NCP** (número de comidas principales por día).  
- **Altura y CH2O** (consumo diario de agua).  
- **Altura y FAF** (frecuencia de actividad física).  

Si bien estos valores son más bajos que los principales, resultan relevantes para complementar la comprensión de los hábitos de los participantes.


```{r Correlacion_categoricas}
# Obtener nombres de columnas categóricas
cat_cols <- names(vars_cat)

# Crear matriz vacía
cramer_mat <- matrix(NA, nrow = length(cat_cols), ncol = length(cat_cols),
                     dimnames = list(cat_cols, cat_cols))

# Calcular Cramér's V
for (i in cat_cols) {
  for (j in cat_cols) {
    cramer_mat[i, j] <- suppressWarnings(assocstats(table(vars_cat[[i]], vars_cat[[j]]))$cramer)
  }
}
round(cramer_mat, 2)


#Haciendo la correlacion Variables categoricas
corrplot(cramer_mat,
         method = "circle",
         type = "upper",
         tl.col = "black",
         tl.srt = 45,
         tl.cex = 0.6
         # addCoef.col = "black"  ← esto lo eliminas o comentas
)


```

#### Asociación entre variables categóricas

En el caso de las variables categóricas, se observa que la variable **NObesity** (tipo de obesidad) presenta las asociaciones más relevantes.  
En particular, se encuentra fuertemente relacionada con:  

- **Género**  
- **Historial familiar con obesidad**  
- **FAVC**: frecuencia de consumo de comida con alto contenido calórico  
- **CAEC**: consumo de alimentos entre comidas principales  

Estos resultados son consistentes con la literatura, ya que el **sexo**, los **antecedentes familiares** y los **hábitos alimenticios** suelen ser predictores importantes en el desarrollo de sobrepeso u obesidad.  
Por lo tanto, estas variables pueden considerarse como **buenos parámetros para identificar y clasificar** si una persona presenta o no obesidad o sobrepeso.



### Análisis de PCA

Con base en el análisis de correlación y asociación realizado previamente, se aplicó la técnica de **Análisis de Componentes Principales (PCA)** con el objetivo de **reducir la dimensionalidad** de los datos y facilitar la interpretación de las variables.

En el caso de las **variables numéricas**, el primer componente principal explica un **26.38%** de la varianza total, mientras que el segundo componente aporta un **18.66%** adicional.  
De esta forma, los tres primeros componentes acumulan un **53.95%** de la varianza explicada.  
Para superar el **90%** de varianza acumulada, sería necesario emplear al menos **7 componentes**, lo que indica que las variables contienen información dispersa que no puede resumirse fácilmente en pocos ejes principales.

Por otro lado, el análisis de PCA con las **variables categóricas transformadas** muestra un comportamiento similar: los dos primeros componentes explican en conjunto un **53.95%** de la varianza acumulada.  

En ambos casos, se decidió emplear únicamente **dos componentes principales**, lo que permite **visualizar los datos en un plano bidimensional**, identificar posibles **clústeres** y analizar la contribución de las variables a dichos componentes.



```{r pca_numericas}
pca2 <- prcomp(var_numeric, scale. = TRUE)
summary(pca2)
```


```{r pca_categoricas}
nombresCat <- rownames(cramer_mat)
pca <- prcomp(cramer_mat, scale. = TRUE)
summary(pca)
```





```{r}
#Grafica del Bracito 
fviz_eig(pca, addlabels = TRUE, ylim = c(0, 100))
```


```{r}
#Grafica del Bracito 
fviz_eig(pca2, addlabels = TRUE, ylim = c(0, 100))
```



### Contribución de variables a los componentes principales

Al analizar la contribución de las variables en los **componentes categóricos**, se observa que las más relevantes en el **primer componente** son:  
- **Historial familiar con obesidad**  
- **NObesity** (nivel de obesidad)  
- **CAEC** (consumo de alimentos entre comidas principales)  
- **FAVC** (consumo de comida con alto contenido calórico)  
- **SMOKE** (hábito de fumar)  
- **Género**

En conjunto, estas variables reflejan los factores **genéticos, de hábitos alimenticios y de estilo de vida** que suelen tener un papel determinante en la obesidad.  

En el caso de las **variables continuas**, el **primer componente** está dominado por:  
- **Altura**  
- **Peso**  
- **CH2O** (consumo de agua al día)  
- **NCP** (número de comidas principales)  
- **FAF** (frecuencia de actividad física)  

Estos resultados indican que las dimensiones más relevantes en el espacio continuo tienen que ver con la **constitución física** de los participantes y algunos aspectos de **hábitos de vida** relacionados con la salud.  

El **segundo componente** incorpora principalmente la **edad** y el **tiempo frente a dispositivos electrónicos (TUE)**, mostrando la importancia de factores asociados al **estilo de vida moderno**.  

En resumen, los resultados del PCA sugieren que:  
- En las **variables categóricas** predominan los factores familiares y de hábitos alimenticios.  
- En las **variables continuas** destacan las características físicas básicas, acompañadas de ciertos comportamientos de salud.  



***Categoricas***

```{r,  fig.width=15, fig.height=5}
# Gráficas individuales
g1 <- fviz_contrib(pca, choice = "var", axes = 1) + ggtitle("Contribucion al componente 1")
g2 <- fviz_contrib(pca, choice = "var", axes = 2) + ggtitle("Contribucion al componente 2")
g3 <- fviz_contrib(pca, choice = "var", axes = 3) + ggtitle("Contribucion al componente 2")


# Gráficas individuales
g12 <- fviz_contrib(pca2, choice = "var", axes = 1) + ggtitle("Contribucion al componente 1")
g22 <- fviz_contrib(pca2, choice = "var", axes = 2) + ggtitle("Contribucion al componente 2")
g23 <- fviz_contrib(pca2, choice = "var", axes = 3) + ggtitle("Contribucion al componente 3")

# Lado a lado



# Lado a lado
(g1 + g2 + g3) / (g12 + g22 + g23)




```


### Cluster con PCA


```{r}
pca_df <- as.data.frame(pca2$x)# Obtenemos las coordenadas del PCA
pca_df$NObeyesdad <- data$NObeyesdad # Agregamos la columna de la clase (NObesity) a los datos proyectados




#Creando la funcion para dividir bien a nuestros datos en un scatter plot 
scatterplot_pca <- function(df, pca, texto){
  # Scatterplot de PC1 vs PC2
  ggplot(df, aes(x = PC1, y = PC2, color = NObeyesdad)) +
  geom_point(size = 3, alpha = 0.7) +
  theme_minimal() +
  labs(title = texto,
       x = paste0("PC1 (", round(summary(pca)$importance[2,1]*100,1), "% varianza)"),
       y = paste0("PC2 (", round(summary(pca)$importance[2,2]*100,1), "% varianza)")) +
  scale_color_brewer(palette = "Set1")   # paleta de colores bonita
}

 scatterplot_pca(pca_df, pca2,  "PCA: separación por NObesity (Continuas)")

#scatterplot_pca(pca_df2, pca,  "PCA: separación por NObesity (Categoricas)")


```


Aunque el PCA y el número de observaciones no permiten una separación completamente clara entre los grupos, se aprecia cierta **tendencia de ordenamiento en el segundo componente (eje Y)**.  

En particular:  
- Los pacientes con **peso insuficiente** y **peso normal** tienden a ubicarse en la parte inferior del gráfico.  
- Conforme ascendemos en el eje Y, se observa un patrón en el que los distintos **tipos y niveles de obesidad** comienzan a ordenarse de manera progresiva.  

Este comportamiento sugiere que los **componentes principales capturan información relevante sobre el grado de obesidad**, aunque la superposición entre grupos indica que no es posible realizar una clasificación perfecta únicamente con estos dos componentes.

### Interpretación del PCA (Biplot)

**Variables continuas**

```{r}
fviz_pca_var(pca2, col.var = "cos2", 
             geom.var = c("arrow", "text"), 
             labelsize = 2, 
             repel = FALSE)
```


En el **primer componente (Dim1, 22.6% de varianza)**, las variables con mayor aporte son **altura**, **peso** y, en menor medida, la **CH2O**.  
Esto es coherente, ya que a mayor altura suele observarse un mayor peso, además, la **FAF (frecuencia de actividad física)** también aparece como una variable relevante, mostrando la importancia de los hábitos de ejercicio en este componente.

En el **segundo componente (Dim2, 18.7% de varianza)**, se aprecia una fuerte correlación negativa entre la **edad** y el **tiempo de uso de dispositivos electrónicos (TUE)**.  
Asimismo, destacan correlaciones negativas entre el **peso** y la **FAF** (actividad física), y entre el **peso** y **CH2O** (consumo de agua).  
Esto indica que, a menor frecuencia de actividad física y menor consumo de agua, el peso tiende a ser más elevado.  

Por último, también se observa la relación entre **edad y peso**, lo cual es consistente con la literatura: a mayor edad, existe mayor probabilidad de presentar sobrepeso u obesidad.  

En conjunto, el PCA refleja tanto la influencia de factores **biológicos** (edad, peso, altura) como de **hábitos de vida** (ejercicio, consumo de agua, tiempo frente a pantallas) en la variabilidad de los datos.

```{r }
fviz_pca_var(pca, col.var = "cos2", 
             geom.var =c("arrow", "text"), 
             labelsize = 2, 
             repel = FALSE) +
  labs(title = "Círculo de correlaciones (PCA)")
```

### Interpretación del PCA (variables categóricas)

En el **primer componente (Dim1, 26.4% de varianza)** se observa una fuerte relación entre:  
- **NObesity** (tipo de obesidad)  
- **Historial familiar con obesidad**  
- **Género**  

Estos factores sugieren que tanto los **antecedentes familiares** como el **sexo** pueden influir en la predisposición a desarrollar sobrepeso u obesidad.  

Por otro lado, también se destacan las variables:  
- **CAEC**: consumo de alimentos entre comidas principales  
- **FAVC**: frecuencia de consumo de comida con alto contenido calórico  

Lo cual refuerza la relevancia de los **hábitos alimenticios** en la clasificación de los niveles de obesidad.  

En conjunto, los resultados muestran que el PCA capta tanto **factores biológicos** (género, antecedentes familiares) como **conductuales** (alimentación) que están estrechamente asociados con el estado de obesidad.


## TSNe



```{r Transformacion_TSNE,  fig.width=15, fig.height=10}

set.seed(42)

# X = features (ya con dummies), y = etiqueta para colorear
X <- data %>% select(-NObeyesdad)
X <- model.matrix(~ . - 1, data = X) %>% as.data.frame()

# escalar
X <- scale(X)

# eliminar filas duplicadas y alinear etiquetas
keep <- !duplicated(X)
X_nd <- X[keep, , drop = FALSE]
y_nd <- data$NObeyesdad[keep]


  tsne <- Rtsne(X_nd, dims = 2,is_distance = FALSE, perplexity = 67,theta=0.3, max_iter = 5000, verbose = TRUE)
    resultados <- as.data.frame(tsne$Y)
    colnames(resultados) <- c("dim_1", "dim_2")
    resultados$NObeyesdad <- y_nd

   ggplot(data = resultados, aes(x = dim_1, y = dim_2, color = NObeyesdad)) +
      geom_point(size = 1.3, alpha = 0.8) +
      scale_color_manual(
      values = c(
        Insufficient_Weight = "#03BCB7",
        Normal_Weight       = "#85D713",
        Obesity_Type_I      = "#F0B000",
        Obesity_Type_II     = "#FF6800",
        Obesity_Type_III    = "#FF4408",
        Overweight_Level_I  = "#C90822",
        Overweight_Level_II = "#800026"
      ),
      drop = FALSE
    ) +
    theme_bw()



#Funcion para graficar varios modelos TSNE
graficaTSNE <- function(perp){
    tsne <- Rtsne(X_nd, dims = 2,is_distance = FALSE, perplexity = perp,theta=0.3, max_iter = 5000, verbose = TRUE)
    resultados <- as.data.frame(tsne$Y)
    colnames(resultados) <- c("dim_1", "dim_2")
    resultados$NObeyesdad <- y_nd

    graph <- ggplot(data = resultados, aes(x = dim_1, y = dim_2, color = NObeyesdad)) +
      geom_point(size = 1.3, alpha = 0.8) +
      scale_color_manual(
      values = c(
        Insufficient_Weight = "#03BCB7",
        Normal_Weight       = "#85D713",
        Obesity_Type_I      = "#F0B000",
        Obesity_Type_II     = "#FF6800",
        Obesity_Type_III    = "#FF4408",
        Overweight_Level_I  = "#C90822",
        Overweight_Level_II = "#800026"
      ),
      drop = FALSE
    ) +
    theme_bw()
    return(graph)
}


```


```{r}

secuencia  <- seq(from = 5, to = 175, by = 10)
secuencia2 <- seq(from = 40, to = 46, by = 2)
secper     <- c(56, 57, 58, 59, 61, 62, 63, 64, 66, 67, 69)

unidas <- c(secuencia, secuencia2, secper)
unidas
rutabase <- getwd()

ruta <- paste0(rutabase, "/FiguresTSNE/")


for(i in unidas){

  nombre <- paste0(ruta,"./TSNE_perp_" , i , ".png")
  
  png(nombre)
  p <- graficaTSNE( i)  # que esta función devuelva un ggplot
  print(p)  
  dev.off()
}




```



```{r  fig.width=15, fig.height=10}
#Usanso PCA y TSNe
pca <- prcomp(x = X_nd)
resultados <- as.data.frame(pca$x[, 1:2])
resultados$NObeyesdad <- y_nd
# Asegura que la clase sea factor y con niveles en el orden que quieres
resultados$NObeyesdad <- factor(
  resultados$NObeyesdad,
  levels = c("Insufficient_Weight","Normal_Weight","Obesity_Type_I",
             "Obesity_Type_II","Obesity_Type_III",
             "Overweight_Level_I","Overweight_Level_II")
)

ggplot(resultados, aes(PC1, PC2, color = NObeyesdad)) +
  geom_point(size = 1.3, alpha = 0.8) +
  scale_color_manual(
    values = c(
      Insufficient_Weight = "#03BCB7",
      Normal_Weight       = "#85D713",
      Obesity_Type_I      = "#F0B000",
      Obesity_Type_II     = "#FF6800",
      Obesity_Type_III    = "#FF4408",
      Overweight_Level_I  = "#C90822",
      Overweight_Level_II = "#800026"
    ),
    drop = FALSE  # conserva colores aunque algún nivel no aparezca
  ) +
  theme_bw()


```




















